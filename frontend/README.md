# Frontend

React + Vite frontend for the Redis DevRel chat demo.

## Package Manager

This frontend is standardized on Bun.

Use:

```bash
bun install
bun run dev
```

Do not use `npm install` for this repo.

## Scripts

```bash
bun run dev
bun run build
bun run lint
bun run preview
```
