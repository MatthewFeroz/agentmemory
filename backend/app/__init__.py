# =============================================================================
# app/__init__.py — Marks the app directory as a Python package
# =============================================================================
# This file is intentionally minimal. Its presence allows Python to treat
# the `app/` directory as a package so we can use imports like:
#   from backend.app.config import get_settings
#   from backend.app.models import ChatRequest
#
# No code is needed here — Python just needs the file to exist.
# =============================================================================
